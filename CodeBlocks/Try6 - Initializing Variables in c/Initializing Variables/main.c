#include <stdio.h>
#include <stdlib.h>

int main()
{
    //printf("Hello world!\n");
    //return 0;
    int jason = 5;
    jason = 8;
    float tom = 23.3334;
    float cash =  19.95;

    double biggie = 55.55555555555555555555555e+11;

    bool boolVariable = true;

    bool ToBe = 1;

    bool notToBe = false;

    short int shorty = 11;

    long int stuff = 99999999999999999L;

    long double US_Deficit_2071 = 1.234e+7L;


    signed short
    unsigned //nonnegative
    unsigned int  //nonnegative numbers

    enum myColor {white, gray, black};

    enum month {January, Febuary, March, April, May, June, July, August, September, October, November, December};
    //enum means the variable month can only be one of the months listed in the brackets. Nothing else can be set to that value.

    enum direction {up, down, left =10, right}; // up === 0, down === 1, left === 10, right === 11;


    char more;
    //anything inside a single quote ; letter character or semicolon

    char broiled;
    broiled = 'T';

    char x = '\n';
}
